# Test Plan

- Operation:
- CPU reference:
- CUDA function:
- Exact small cases:
- Boundary cases:
- Random cases and seed:
- Tolerance:
- Expected failure report fields:
- Integration test:

Pass only when CUDA baseline and optimized CUDA output both match the CPU reference.
